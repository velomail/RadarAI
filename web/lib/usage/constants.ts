export const FREE_DAILY_QUERY_LIMIT = 5;
export const GUEST_DAILY_QUERY_LIMIT = 1;
/** Higher bar for guest preview — surface one strong match. */
export const GUEST_MIN_MATCH_SCORE = 75;
/** Full ranked list for signed-in searches (see post-process MAX_REPORT_JOBS). */
export const AUTH_MAX_REPORT_JOBS = 12;
export const AUTH_MIN_REPORT_JOBS = 3;
