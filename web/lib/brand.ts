/** Public product name — use for UI, emails, and metadata. */
export const APP_NAME = 'RadarAI';

export const APP_TAGLINE = 'AI job search on demand';

export const DEFAULT_EMAIL_FROM = `${APP_NAME} <onboarding@resend.dev>`;
